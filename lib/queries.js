async function (connection, ) {
    const SQL_STATEMENT = ``;

    try {
        const [rows, fields] = await connection.promise().query(SQL_STATEMENT, []);
        console.table(rows);
    } catch (error) {
        console.log(error);
    }
}

async function (connection) {
    const SQL_STATEMENT = ``;

    try {
        const [rows, fields] = await connection.promise().query(SQL_STATEMENT);
        console.table(rows);
    } catch (error) {
        console.log(error);
    }
}

async function (connection, ) {
    const SQL_STATEMENT = ``;

    try {
        const [rows, fields] = await connection.promise().query(SQL_STATEMENT, []);
        console.table(rows);
    } catch (error) {
        console.log(error);
    }
}

async function (connection, ) {
    const SQL_STATEMENT = ``;

    try {
        const [rows, fields] = await connection.promise().query(SQL_STATEMENT, );
        console.table(rows);
    } catch (error) {
        console.log(error);
    }
}

async function (connection, ) {
    const SQL_STATEMENT = ``;

    try {
        const [rows, fields] = await connection.promise().query(SQL_STATEMENT, []);
        console.table(rows);
    } catch (error) {
        console.log(error);
    }
}

module.exports = {
    
};